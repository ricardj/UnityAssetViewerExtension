Ideas for the future:

Features:

- Refactor the chrome extension so it supports: Gitlab/GitHub providers. Both Static file viewing and through MR reviewing. AlsoConsider Bitbucket.
- Introduce basic 3d support to the prefab visualization
- Introduce some visual layout to overlay to indicate git changes in the prefab
- Introduce support for basic Material Rendering probably using three.js

Fixes:
- Prefab variant content is not nested to the parent in the visualization